<Window x:Class="PowerPointRemote.DesktopClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PowerPointRemote.DesktopClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />

        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBox x:Name="ChannelUri" Text="{Binding ChannelUri, Mode=OneWay}" IsReadOnly="True" />
            <Button Content="Copy" Command="{Binding CopyTextBoxCommand}"
                    CommandParameter="{Binding Text, ElementName=ChannelUri}" />
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding ConnectedUsers}" DisplayMemberPath="Name" />
        <Button Grid.Row="2" Content="New Channel" Command="{Binding NewChannelCommand}" Height="32" Margin="5" />
    </Grid>
</Window>