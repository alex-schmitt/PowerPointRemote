<UserControl x:Class="PresenterClient.Views.ChannelUsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="0 15 0 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label FontSize="14" Content="Connections" HorizontalAlignment="Center" />
        <Label Grid.Row="1" Visibility="{Binding WaitingVisibility}" Content="Waiting for users to connect"
               HorizontalAlignment="Center" />
        <ListBox Grid.Row="2" ItemsSource="{Binding ChannelUsers}" DisplayMemberPath="Name" BorderThickness="0"
                 Margin="18 2" />
    </Grid>
</UserControl>